{
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@rails/ujs": "^7.1.0",
    "htmx.org": "^2.0.0",
    "modern-normalize": "^2.0.0"
  },
  "scripts": {
    "heroku-postbuild": "npm run build",
    "build": "npx vite build",
    "watch": "npx vite build --watch",
    "production": "npm run build && crystal build src/start_server.cr -o bin/server --release --stats && LUCKY_ENV=production ./bin/server"
  },
  "devDependencies": {
    "@babel/compat-data": "^7.23.5",
    "@tailwindcss/oxide": "^4.0.17",
    "@tailwindcss/postcss": "^4.0.17",
    "lightningcss": "^1.29.3",
    "postcss": "^8.5.3",
    "sass": "^1.69.5",
    "tailwindcss": "^4.0.17",
    "vite": "^6.2.5",
    "vite-plugin-lucky": "^0.2.0"
  }
}
